{
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require ./steps/**/*.ts --require ./hooks/**/*.ts --format json:report.json --exit"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.1",
    "@cucumber/pretty-formatter": "^1.0.0",
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
